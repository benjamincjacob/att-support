---
declared-services:
  conversation-service:
    label: conversation
    plan: free
applications:
- name: att-demo-chat
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - Conversation-att
  env:
    NPM_CONFIG_PRODUCTION: false
